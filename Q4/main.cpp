#include <bits/stdc++.h>

using namespace std;


int main() {
    // unordered between 0 -> n
    // no dupes -> set
    // O(1) add, delete, search
    // O(n) for clearing list

    // Maybe linked list index to or hash function to number

    return 1;
}
